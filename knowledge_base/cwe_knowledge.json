[
  {
    "cwe_id": "CWE-120",
    "name": "Buffer Copy without Checking Size of Input",
    "name_cn": "缓冲区溢出",
    "severity": "High",
    "description": "The program copies an input buffer to an output buffer without verifying that the size of the input buffer is less than the size of the output buffer.",
    "detection_patterns": [
      "strcpy without length check",
      "memcpy with unchecked size",
      "gets()",
      "sprintf without bounds"
    ],
    "typical_fix": "Use bounded copy functions (strncpy, snprintf) or validate input length before copy.",
    "related_cwes": [
      "CWE-119",
      "CWE-787",
      "CWE-788"
    ]
  },
  {
    "cwe_id": "CWE-134",
    "name": "Use of Externally-Controlled Format String",
    "name_cn": "格式化字符串漏洞",
    "severity": "High",
    "description": "The software uses a function that accepts a format string as an argument, but the format string originates from an external source.",
    "detection_patterns": [
      "printf(user_input)",
      "fprintf(fp, var)",
      "sprintf(buf, var)"
    ],
    "typical_fix": "Always use a static format string: printf(\"%s\", user_input).",
    "related_cwes": [
      "CWE-20"
    ]
  },
  {
    "cwe_id": "CWE-190",
    "name": "Integer Overflow or Wraparound",
    "name_cn": "整数溢出",
    "severity": "High",
    "description": "The software performs a calculation that can produce an integer overflow, leading to unexpected behavior.",
    "detection_patterns": [
      "multiplication without overflow check",
      "size_t wraparound",
      "count * sizeof"
    ],
    "typical_fix": "Check for overflow before arithmetic operations, use safe integer libraries.",
    "related_cwes": [
      "CWE-191",
      "CWE-681"
    ]
  },
  {
    "cwe_id": "CWE-416",
    "name": "Use After Free",
    "name_cn": "释放后使用",
    "severity": "High",
    "description": "Referencing memory after it has been freed can cause program crashes or arbitrary code execution.",
    "detection_patterns": [
      "free(p) then p->field",
      "dangling pointer access",
      "double free"
    ],
    "typical_fix": "Set pointer to NULL after free; use smart pointers in C++.",
    "related_cwes": [
      "CWE-415",
      "CWE-825"
    ]
  },
  {
    "cwe_id": "CWE-476",
    "name": "NULL Pointer Dereference",
    "name_cn": "空指针解引用",
    "severity": "Medium",
    "description": "A NULL pointer dereference occurs when the application dereferences a pointer that it expects to be valid, but is NULL.",
    "detection_patterns": [
      "pointer access without NULL check",
      "malloc without NULL check",
      "function return without validation"
    ],
    "typical_fix": "Always check pointers for NULL before dereferencing.",
    "related_cwes": [
      "CWE-252",
      "CWE-690"
    ]
  }
]